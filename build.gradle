group 'com.testobject'
version '1.0-SNAPSHOT'

apply plugin: 'java'

sourceCompatibility = 1.5

repositories {
    mavenCentral()
}

test {
    testLogging {
        showExceptions = true
    }
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.11'

    compile 'io.appium:java-client:3.1.0'
    compile 'org.seleniumhq.selenium:selenium-server:2.25.0'

}
